<Page x:Class="MZikmund.Toolkit.Sample.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:MZikmund.Toolkit.Sample">

  <ScrollViewer Padding="24">
    <StackPanel Spacing="16" MaxWidth="800">
      <TextBlock Text="MZikmund Toolkit Gallery"
                 Style="{ThemeResource TitleTextBlockStyle}" />
      
      <TextBlock Text="Welcome to the MZikmund Toolkit Gallery! This sample application demonstrates the features available in the MZikmund.Toolkit.WinUI library."
                 TextWrapping="Wrap"
                 Style="{ThemeResource BodyTextBlockStyle}" />
      
      <TextBlock Text="Features" 
                 Style="{ThemeResource SubtitleTextBlockStyle}"
                 Margin="0,16,0,8" />
      
      <StackPanel Spacing="8">
        <TextBlock Style="{ThemeResource BodyTextBlockStyle}">
          <Run Text="• " FontWeight="Bold" />
          <Run Text="Preferences Service:" FontWeight="SemiBold" />
          <Run Text=" A service for storing and retrieving application settings with support for simple and complex types." />
        </TextBlock>
        
        <TextBlock Style="{ThemeResource BodyTextBlockStyle}">
          <Run Text="• " FontWeight="Bold" />
          <Run Text="Dialog Coordinator:" FontWeight="SemiBold" />
          <Run Text=" Coordinates ContentDialog display to ensure only one dialog is shown at a time." />
        </TextBlock>
        
        <TextBlock Style="{ThemeResource BodyTextBlockStyle}">
          <Run Text="• " FontWeight="Bold" />
          <Run Text="Package Version Extensions:" FontWeight="SemiBold" />
          <Run Text=" Helper extensions for formatting PackageVersion objects." />
        </TextBlock>

        <TextBlock Style="{ThemeResource BodyTextBlockStyle}">
          <Run Text="• " FontWeight="Bold" />
          <Run Text="ObservableCollection Merge:" FontWeight="SemiBold" />
          <Run Text=" MergeWith extension that synchronizes an ObservableCollection with a source list using minimal Insert/RemoveAt/Move operations." />
        </TextBlock>

        <TextBlock Style="{ThemeResource BodyTextBlockStyle}">
          <Run Text="• " FontWeight="Bold" />
          <Run Text="Notification Permission:" FontWeight="SemiBold" />
          <Run Text=" INotificationPermissionService cross-platform wrapper for query / request / open-settings flow." />
        </TextBlock>
        
        <TextBlock Style="{ThemeResource BodyTextBlockStyle}">
          <Run Text="• " FontWeight="Bold" />
          <Run Text="XamlRoot Provider:" FontWeight="SemiBold" />
          <Run Text=" Provides centralized access to the XamlRoot instance for dialogs and popups." />
        </TextBlock>
      </StackPanel>
      
      <TextBlock Text="Navigation"
                 Style="{ThemeResource SubtitleTextBlockStyle}"
                 Margin="0,16,0,8" />
      
      <TextBlock Text="Use the navigation menu on the left to explore individual samples for each feature."
                 TextWrapping="Wrap"
                 Style="{ThemeResource BodyTextBlockStyle}" />
    </StackPanel>
  </ScrollViewer>
</Page>
